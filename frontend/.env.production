# Production builds (Vercel) call the VM API over its Tailscale HTTPS endpoint.
# Only devices logged into Max's tailnet can resolve/reach this host — the API is
# not public. Port 443 on the VM is the fitness tracker's Funnel; PBS is on 8443.
VITE_API_BASE=https://maxlab.tailab5a2d.ts.net:8443
