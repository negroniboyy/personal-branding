# RETIRED — see the single source of truth

This per-repo checkpoint is **no longer maintained**. As of 2026-07-04 the BrandStudio
workspace has ONE live checkpoint, at the workspace root:

- **`../../md/checkpoint.md`** (BrandStudio root) — session state
- **`../../md/code_index.md`** (BrandStudio root) — code map

`/session-checkpoint` writes to the root `md/` only. Do not update this file; do not create a
new checkpoint here. Read the root pair to bootstrap.
