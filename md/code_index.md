# RETIRED — see the single source of truth

This per-repo code index is **no longer maintained**. As of 2026-07-04 the BrandStudio
workspace has ONE live code map, at the workspace root:

- **`../../md/code_index.md`** (BrandStudio root) — code map (personal_brand deep map folded in)
- **`../../md/checkpoint.md`** (BrandStudio root) — session state

`/session-checkpoint` writes to the root `md/` only. Do not update this file; do not create a
new index here. Read the root pair to bootstrap.
